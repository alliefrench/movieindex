{
  "name": "movieindex-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "scripts": {
    "start": "echo \"Add a React build setup (e.g. CRA, Vite)\" && exit 1"
  }
}
